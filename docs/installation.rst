============
Installation
============

At the command line::

    $ easy_install dj-newsletter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-newsletter
    $ pip install dj-newsletter
